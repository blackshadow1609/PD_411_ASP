@page "/factorial"
@rendermode InteractiveServer
@using System.Numerics

<PageTitle>Factorial</PageTitle>

<h3>Factorial</h3>

<div class="factorial">
    <input type="number" min="0" @bind="number" @oninput="Calculate" />
    <div id="factorial"> @factorial </div>
</div>

@code {
    int number;
    BigInteger factorial = 1;
    string errorMessage = "";
    
    void Calculate()
    {
        if (number < 0)
        {
            errorMessage = "Факториал отрицательного числа не определен";
            factorial = 0;
            return;
        }
        
        errorMessage = "";
        factorial = 1;
        for (int i = 1; i <= number; i++)
        {
            factorial *= i;
        }
    }
}